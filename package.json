{
  "name": "world-inflation-lens",
  "private": true,
  "version": "0.8.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "verify": "node scripts/verify-external.mjs && node scripts/verify-cbo.mjs && node scripts/verify-dollar.mjs && node scripts/verify-refresh.mjs && node scripts/verify.mjs",
    "data:refresh": "node scripts/refresh-data.mjs"
  },
  "dependencies": {
    "d3-geo": "^3.1.1",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "vite": "^7.1.7"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
