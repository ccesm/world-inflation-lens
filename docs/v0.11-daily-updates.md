# V0.11 — Homepage layout repair and daily notifications

## Reproduced bug and fix

The homepage framework had seven cards but a desktop grid hard-coded for six cards with narrow separator columns. At 1440 pixels the seventh card occupied a roughly 30-pixel column and became over 1100 pixels tall. The earlier small-screen checks did not cover this wide-screen failure.

Cards are now the only grid items; decorative plus signs belong inside cards. A responsive equal-width grid wraps based on available space and no longer depends on the number of forces. All seven forces, links and methodology remain. Before/after browser measurements showed the seventh card change from 30 pixels to approximately 291 pixels wide at the same desktop viewport.

The daily-check health threshold is now 48 hours (previously ten days). Invalid or future check timestamps are also marked overdue instead of appearing healthy.

## Daily official-source updates

The existing GitHub Pages workflow now runs every day at **08:00 America/Los_Angeles**. GitHub's timezone-aware schedule handles daylight saving time. Runs may be delayed; this is a scheduled check, not a guarantee of exact-time data delivery. See [GitHub schedule documentation](https://docs.github.com/en/actions/reference/workflows-and-actions/events-that-trigger-workflows#schedule).

The task checks the existing official/public endpoints and publishes only after full validation, build and verification. Original monthly/quarterly/annual frequencies remain unchanged. CBO projections, SIPRI annual releases and manually reviewed stablecoin publication estimates retain their reviewed versions. This task does not scrape general news or generate macro forecasts.

The original atomic snapshot replacement, revision ledger and rollback are retained. Pushes to main still deploy without fetching data. Manual workflow dispatch with `refresh: true` performs an immediate data check and notification. The workflow's time budget is raised to 30 minutes to accommodate source retries.

## Email reminders — setup required

The sending implementation uses Gmail SMTP through Nodemailer on the GitHub runner. The same personal Gmail address is both sender and recipient. No mail credentials are bundled in the frontend.

Configure these **repository Actions secrets**:

| Secret | Meaning |
| --- | --- |
| `GMAIL_ADDRESS` | Your personal `@gmail.com` address; sends to itself |
| `GMAIL_APP_PASSWORD` | The 16-character Google app password for this project; not your normal Gmail password |

On an iPhone, open [Google Account security](https://myaccount.google.com/security) in Safari, turn on **2-Step Verification**, then open [App passwords](https://myaccount.google.com/apppasswords) and create a password named “World Inflation Lens.” Copy the generated 16-character password. If App passwords is unavailable, check [Google's eligibility rules](https://support.google.com/accounts/answer/185833?hl=en); some account protections and organization settings disable it. Never use your regular Google password here.

In Safari, open the [repository's Actions secrets page](https://github.com/ccesm/world-inflation-lens/settings/secrets/actions), sign in as a repository administrator, tap **New repository secret**, and create both secrets exactly as named above. For `GMAIL_ADDRESS`, enter the Gmail address you want to receive mail at; for `GMAIL_APP_PASSWORD`, enter the generated app password. On a narrow screen, Safari's **Request Desktop Website** can expose hidden settings controls. Do not put either value in source files, `VITE_` variables, issue comments or chat. Previously used Resend secrets are no longer read by the workflow and may be removed from the repository settings.

Mail runs after both build and deployment for daily checks and refresh-enabled manual runs. Normal code pushes do not send mail. The mail job checks out the precise snapshot commit used by the build. It distinguishes successful deployment with changed observations, a successful unchanged check, and incomplete updates/deployment. A ledger entry must match the current GitHub run ID to support a success claim. An old successful record cannot be presented as today's result.

The bilingual plain-text digest includes revision counts, a few source-specific latest observation dates and units, and links to the website and current run. Failure notices explicitly avoid new-data claims. The address and app password are not logged. SMTP sends are attempted only once because an uncertain response might already have delivered the message; automatic retries could duplicate it. Gmail's SMTP acceptance is not proof of inbox delivery. A deliberate workflow rerun can send another notice. Gmail may challenge or block sign-in from an unfamiliar GitHub runner, so the first configured run needs an inbox and Actions-log check.

When secrets are absent, the job clearly warns **email not sent** and leaves data deployment available. No email has been sent during development. End-to-end delivery cannot be verified until the Gmail address and app password are configured.

`npm run notify:preview` renders a safe preview without reading mail configuration or making a sending request. With no workflow context it intentionally demonstrates the incomplete-run notice. Unit tests separately exercise successful and unchanged results with fixtures.

## Verification

- `npm run build`: PASS. Existing nonfatal large-bundle warnings remain.
- `npm run verify`: PASS, including all 19 routes in English/Chinese, CBO boundaries, source validation, revisions and rollback.
- Notification tests: success/unchanged/failure, stale ledger, absent secrets, same-address delivery, a single SMTP attempt, stable Message-ID and sanitized errors; mocked transport only.
- Browser: framework geometry at 1440, 1280, 1024, 768, 390 and 320 pixels; English and Chinese; light/dark modes. All seven cards have usable widths and no horizontal document overflow. Desktop screenshot reviewed.
- A real online refresh succeeded at **2026-09-22 19:39:22 UTC**: 24 new observations and 31 revised values, with no filled or withdrawn values. M2, yields, dollar index, Fed assets, electricity/semiconductor output and bank deposits contributed changes. After merging the remote September 21 automated snapshot, a second successful check at **19:44:22 UTC** recorded **9 additional observations and 24 revisions relative to that remote baseline**. The remote run and its revision history are retained. Both refreshes completed their own build/verify gates. Current observations and retrieval metadata were updated; reviewed CBO/SIPRI/publication vintages, calculations, legacy hashes and CSV functionality remain intact.

## Files

Frontend: `src/components/ResearchFramework.jsx`, `src/architecture.css`, `src/components/DataStatus.jsx`, `src/utils/dataStatus.js`, `src/i18n/updates.js`, `src/i18n/digitalMoney.js`.

Automation: `.github/workflows/deploy.yml`, `scripts/refresh-data.mjs`, `scripts/lib/notification.mjs`, `scripts/notify.mjs`, `scripts/verify-notification.mjs`, `scripts/verify-refresh.mjs`, `scripts/verify.mjs`.

Refreshed snapshots: `data/inflation/{fred,drivers,monitor,worldbank}.json`, `data/external/{gpr,gscpi,fao-food}.json`, `data/productivity/series.json`, `data/digital-money/bank-deposits.json`, `data/updates/history.json`.

Release: `package.json`, `package-lock.json`, `README.md`, `docs/roadmap.md`, this report.

Next: route-level loading to reduce homepage payload, followed by data-vintage comparisons. Additional datasets are not required for this repair.
